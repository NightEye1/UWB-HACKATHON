import { Map, Hammer, HeartPulse, Flame, FileBadge, Network, type LucideIcon } from "lucide-react";

export const agentIcons: Record<string, LucideIcon> = {
  map: Map,
  hammer: Hammer,
  heart: HeartPulse,
  flame: Flame,
  badge: FileBadge,
  network: Network,
};
